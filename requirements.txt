# Phase 1 — core pipeline (install these to start)
langgraph>=0.2
google-genai>=1.0          # official Gemini SDK (google-generativeai is deprecated — do not use it)
pandas>=2.0
python-dotenv>=1.0
gradio>=4.0

# Phase 2 — real integrations
requests>=2.31          # AbuseIPDB lookups + Slack webhook posts

# Phase 3 — GraphRAG
neo4j>=5.20             # graph store driver
voyageai>=0.2           # embeddings for hybrid retrieval

# Phase 4 — persistent memory
langgraph-checkpoint-sqlite>=2.0   # persistent LangGraph checkpointer (investigation_history itself uses stdlib sqlite3, no extra dependency)

# Dev / testing
pytest>=8.0
